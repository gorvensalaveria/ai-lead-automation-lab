services:
  lead-intake:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_ENV: development
    volumes:
      - ./data/outputs:/app/data/outputs
      - ./logs:/app/logs
    restart: unless-stopped
